{
  "name": "observability-api",
  "version": "1.0.0",
  "description": "API de métricas do servidor (CPU, RAM, disco, n8n) para o painel de Observabilidade",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": { "node": ">=18" }
}
